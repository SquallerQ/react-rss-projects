import { FC } from 'react';

const HookForm: FC = () => {
  return (
    <form>
      <h2>React Hook Form</h2>
      <button type="submit">Submit</button>
    </form>
  );
};

export default HookForm;
